function sum(number1: number, number2: number) {
    return number1 + number2
}

sum(1, 2)

const movies = ['Batman vs Superman', 'Senhor dos Anéis', 'Vingadores']

movies.forEach((movie) => {
    console.log(movie);
})