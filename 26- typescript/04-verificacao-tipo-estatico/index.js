function sum(number1, number2) {
    return number1 + number2;
}
sum(1, 2);
var movies = ['Batman vs Superman', 'Senhor dos Anéis', 'Vingadores'];
movies.forEach(function (movie) {
    console.log(movie);
});
